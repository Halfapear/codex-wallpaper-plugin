# Contributing

Issues and pull requests are welcome.

Please keep these rules in mind:

- Do not commit generated CSS files that contain local image data.
- Do not commit private screenshots or local file paths.
- Keep selectors narrow. Avoid broad selectors such as `[class*="input"]` that can break the composer.
- Verify changes in a running Codex Desktop window before opening a pull request.
